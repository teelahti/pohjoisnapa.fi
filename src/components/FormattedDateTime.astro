---
interface Props {
  value?: string;
}
const { value } = Astro.props;
import formats from "./dateformats.ts";

if (!value) {
  return false;
}
---

<time>
  {formats(Astro.currentLocale ?? "fi").full.format(new Date(value))}
</time>
